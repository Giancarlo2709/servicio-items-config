spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_spring_cloud?serverTimezone=America/Lima
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG